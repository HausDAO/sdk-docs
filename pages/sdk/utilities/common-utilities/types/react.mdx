# [`react.ts`](https://github.com/HausDAO/daohaus-monorepo/blob/develop/libs/common-utilities/src/types/react.ts)

Creates a type alias called `ReactSetter` which is a dispatch of a `SetStateAction` of generic type `T`.

## Types

- `ReactSetter<T>`

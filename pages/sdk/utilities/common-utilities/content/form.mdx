# [`form.ts`](https://github.com/HausDAO/daohaus-monorepo/blob/develop/libs/common-utilities/src/content/form.ts)

Defines a constant named `FORM_COPY`. The value of this constant is an object that contains properties named `TOKENS`, `TIMING`, `ADVANCED`, `SHAMANS`, and `MEMBERS`. Each of these properties has a title and description.

## Constants

- `FORM_COPY`
